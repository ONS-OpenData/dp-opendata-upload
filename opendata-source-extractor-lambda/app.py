def handler(event, context):
    """
    Principle lambda event handler.
    """

    # Validate event matches schema transform_evocation_payload_schema

    # Use the "source" field to get the zip from the initial bucket.

    # Get the v4 out of it.

    # Upload the v4 to the dev bucket AND add our attribute specifying where to find the zip file.
